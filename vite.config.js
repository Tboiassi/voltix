import { defineConfig } from 'vite'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [
    tailwindcss(),
  ],
  // base: 'https://github.com/Tboiassi/Voltix-web.git', // Replace with your exact GitHub repository name
  // ... rest of your config
})